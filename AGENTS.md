## Agent skills

### Issue tracker

问题使用 GitHub Issues 管理。参见 `docs/agents/issue-tracker.md`。

### Triage labels

使用默认的五个分诊标签。参见 `docs/agents/triage-labels.md`。

### Domain docs

采用单上下文领域文档布局。参见 `docs/agents/domain.md`。
