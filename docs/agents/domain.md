# 领域文档

工程技能在探索代码库前，应按相关性阅读以下已存在的文档：

- 根目录 `CONTEXT.md`；若同时存在 `CONTEXT-MAP.md`，则按其指引选择对应上下文；
- `docs/architecture/adr/`（若存在）中涉及当前工作区域的架构决策记录（ADR）。

当前仓库尚未启用正式领域文档布局。上述文档不存在时应静默继续；是否新建领域上下文或 ADR，应由维护者明确决定，并同步调整 `.gitignore`。

相关文档存在时，输出中的领域概念应优先采用 `CONTEXT.md` 定义的术语。若某项建议或实现与既有 ADR 冲突，必须显式说明冲突及原因。
