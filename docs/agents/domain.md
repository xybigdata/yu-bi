# 领域文档

工程技能在探索代码库前，应按相关性阅读以下文档：

- 根目录 `CONTEXT.md`；若存在 `CONTEXT-MAP.md`，则按其指引选择对应上下文；
- `docs/architecture/adr/` 中涉及当前工作区域的架构决策记录（ADR）。

当前仓库采用单上下文领域文档布局。若上述领域文档尚不存在，应静默继续；仅在领域术语或架构决策明确时再创建或更新。

输出中的领域概念应优先采用 `CONTEXT.md` 定义的术语。若某项建议或实现与既有 ADR 冲突，必须显式说明冲突及原因。
